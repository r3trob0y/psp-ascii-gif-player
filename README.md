# ASCII Video Player для PSP

[![PSP Version](https://img.shields.io/badge/PSP-6.60%2B-orange)](https://www.playstation.com/psp)
[![Python](https://img.shields.io/badge/Python-3.8%2B-blue)](https://python.org)
[![License](https://img.shields.io/badge/License-MIT-green)](LICENSE)

Проигрыватель ASCII-анимаций с аудиосопровождением для Sony PSP. Конвертирует GIF-анимации и аудиофайлы в оптимизированный формат для демо-сцен или развлекательного контента на портативной консоли.

## ✨ Особенности

- **Конвертация GIF в ASCII** с настраиваемой палитрой символов
- **Аудио поддержка** (WAV, MP3, OGG, FLAC) с автоматической конвертацией
- **Автоматическое масштабирование** с сохранением пропорций
- **Поддержка циклов** и настраиваемой скорости воспроизведения
- **Простой конфигурационный файл** для тонкой настройки
- **Готовые бинарники** для Windows и PSP

## 📁 Структура проекта

```
PSP-ASCII-Player/
├── converter.py              # Конвертер GIF+аудио в формат для PSP
├── main.c                    # Основной код проигрывателя для PSP
├── audio/                    # Аудио-библиотека для PSP
│   └── pspaalib.h
├── compiled/
│   ├── ascii_player.elf      # Исполняемый файл для PSP
│   ├── ascii_player.pbp      # Запускаемый PBP-файл
│   └── converter.exe         # Конвертер для Windows
├── config.ini                # Пример конфигурации
├── output/
│   ├── animation.dat         # Конвертированная анимация
│   └── sound.wav             # Конвертированное аудио
└── README.md                 # Этот файл
```

## 🚀 Быстрый старт

### Вариант 1: Использование готовых бинарников (рекомендуется)

1. **Для конвертации на Windows:**
   - Запустите `compiled/converter.exe`
   - Выберите GIF-файл с анимацией
   - Выберите аудиофайл (WAV, MP3, OGG, FLAC)
   - Программа создаст три файла:
     - `animation.dat` - конвертированная ASCII-анимация
     - `sound.wav` - конвертированное аудио
     - `config.ini` - конфигурационный файл

2. **Для запуска на PSP:**
   - Скопируйте на PSP в папку `/PSP/GAME/`:
     - `ascii_player.pbp` (из папки `compiled/`)
     - `config.ini`
     - `animation.dat`
     - `sound.wav`
   - Запустите программу из меню PSP

### Вариант 2: Сборка из исходников

#### Требования для сборки конвертера

```bash
# Требуемые Python-пакеты
pip install pillow pydub tkinter

# Для поддержки форматов кроме WAV установите FFmpeg
# Windows: скачайте с ffmpeg.org и добавьте в PATH
# Linux: sudo apt install ffmpeg
# macOS: brew install ffmpeg
```

#### Сборка конвертера

```bash
# Просто запустите Python-скрипт
python converter.py

# Или соберите в EXE (опционально)
pip install pyinstaller
pyinstaller --onefile --name "ascii_converter" converter.py
```

#### Сборка для PSP

1. **Установите PSP SDK:**
   - Следуйте инструкциям с [pspdev.github.io](https://pspdev.github.io/)

2. **Соберите проект:**

```bash
# Клонируйте репозиторий
git clone https://github.com/yourusername/PSP-ASCII-Player.git
cd PSP-ASCII-Player

# Компиляция
psp-gcc -O2 -G0 -Wall -D_PSP_FW_VERSION=660 \
  main.c -L. -lpspaalib -lpspgu -lpspdisplay -lpspdebug -lpspctrl -lpspkernel \
  -o ascii_player.elf

# Создание PBP-файла
psp-fixup-imports ascii_player.elf
psp-strip ascii_player.elf
mksfoex -d MEMSIZE=1 "ASCII Player" PARAM.SFO
pack-pbp EBOOT.PBP PARAM.SFO NULL NULL NULL NULL NULL ascii_player.elf NULL
```

## ⚙️ Конфигурация

Файл `config.ini` позволяет настроить воспроизведение:

```ini
[Animation]
File = animation.dat        # Файл с анимацией

[Audio]
File = sound.wav           # Аудиофайл
Volume = 100               # Громкость (0-100)

[Display]
FrameDelay = 3             # Задержка между кадрами
Loop = 1                   # Зацикливание (0/1)
```

## 🎨 Настройка ASCII-палитры

Вы можете изменить палитру символов в файле `converter.py`:

```python
# Палитра от темного к светлому
ASCII_CHARS = "   :;i1tfrxvunzjJYLQ0OZmwqpkhao*MW&%B8#@"
DARK_THRESHOLD = 40        # Порог для черного цвета
```

## 🎮 Управление на PSP

- **Start** - выход из программы
- Остальные кнопки не используются (можно расширить)

## 📊 Технические детали

### Формат файла animation.dat
```
[Заголовок - 8 байт]
  uint32: количество кадров
  uint16: ширина (60)
  uint16: высота (34)

[Данные кадров]
  Каждый кадр: 60×34 символов + нуль-терминаторы строк
  Размер кадра: (60+1) × 34 = 2074 байт
```

### Аудио требования
- Частота: 44100 Hz
- Формат: 16-bit Stereo
- Контейнер: WAV

## 🐛 Известные ограничения

1. **Размер анимации:** фиксированный 60×34 символов (размер экрана отладки PSP)
2. **Цвета:** только черно-белая ASCII-графика
3. **Память:** большие анимации могут вызвать нехватку памяти
4. **Аудио:** поддерживаются только WAV файлы на PSP

## 🔧 Устранение проблем

### Конвертер не запускается
- Убедитесь, что установлен Python 3.8+
- Установите требуемые пакеты: `pip install pillow pydub`
- Для форматов кроме WAV установите FFmpeg

### Нет звука на PSP
- Проверьте, что аудио сконвертировано в 44100Hz, 16-bit Stereo WAV
- Убедитесь, что файл называется так же, как в config.ini

### Анимация не отображается
- Проверьте размер GIF (рекомендуется не более 100 кадров)
- Убедитесь, что файл `animation.dat` скопирован на PSP

## 📝 Примеры использования

1. **Музыкальные визуализаторы** - синхронизация анимации с музыкой
2. **Демо-сцены** - создание ASCII-демо для PSP
3. **Конвертация видео** - преобразование коротких видеороликов
4. **Искусство ASCII** - цифровое искусство в текстовом формате

## 🤝 Вклад в проект

Приветствуются пул-реквесты и issue! Особенно интересны:
- Поддержка цветной ASCII-графики
- Оптимизация использования памяти
- Дополнительные форматы ввода
- Улучшение алгоритма дизеринга

## 📄 Лицензия

Смотрите файл [LICENSE](LICENSE) для подробностей.

*Создано с ❤️ для сообщества PSP. Работает на PSP с прошивкой 6.60 и выше.*